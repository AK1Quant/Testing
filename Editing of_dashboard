Bootstrap 4 Modal (bs4Dash)

# bs4Dash Modal
bs4Dash::bs4Modal(
  id = "popUpModal",
  title = "This is a Bootstrap Modal",
  size = "lg",
  footer = tagList(
    modalButton("Close")
  ),
  body = "Here is the content of the pop-up."
)

Button to Trigger Modal

actionButton("showModal", "Click to Show Pop-up")

Show Modal on Button Click

observeEvent(input$showModal, {
  bs4Dash::showModal("popUpModal")
})
