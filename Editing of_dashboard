library(shinyjs)
```

```{r popup-ui, include=FALSE}
# Define a modal popup
popupModal <- function() {
  modalDialog(
    title = "Welcome to the Dashboard",
    p("Click the button below to access the dashboard."),
    footer = tagList(
      modalButton("Close"),
      actionButton("enter_btn", "Enter", class = "btn-primary")
    ),
    easyClose = FALSE,
    fade = TRUE
  )
}
```

```{r}
# User Interface
ui <- shinyUI(fluidPage(
  useShinyjs(),  # Initialize shinyjs
  hidden(div(id = "dashboard_content", 
    flexdashboard::flex_dashboardPage(
      # Add your flexdashboard content here
      column(width = 6, "Main content goes here")
    )
  ))
))
